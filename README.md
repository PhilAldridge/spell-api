# spell-api

## Setup

- Run a MySQL server locally (e.g. using docker)
- Fill in .env file with your MySQL server details.
- Install go from the golang website
- Run all commands in `get-packages.sh` to install necessary packages
- Run command `go run ./cmd/api/main.go` to run the api server
